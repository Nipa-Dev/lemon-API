# backend, optional value does not need to be specified.
# Default value is False, this variable is only used to import custom testing endpoint
DEBUG = False
# to get secret key run: openssl rand -hex 32 or optionally use this but not for production
SECRET_KEY = c8625b1e9b48c9fabe2a12522bd1cb59222ce092a1d12cfeb915d8e7840efac2

# This uses the test data files for populating database
# You can add your own path and the content of files (.md, .txt) will be added to database
IMPORT_NOTES_PATH=./tests/data


# Database, connecting to docker PostgreSQL container
DB_PORT = 5432
DB_USER = admin
DB_PASSWORD = admin
DB_NAME = lemonapi
